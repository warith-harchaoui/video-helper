# video-helper — pip requirements (portable, no `file://` deps).
#
# For a plain install:
#     pip install -r requirements.txt
#     pip install -e .           # video-helper itself (editable) or `pip install .`
#
# Optional surfaces (CLI/API/MCP) live behind extras, e.g. `.[cli]`, `.[api,mcp]`.
# The os-helper pin is kept in lock-step with the release tag below.

# --- Suite dependency -----------------------------------------------------
os-helper>=1.4.2
# --- Core runtime ---------------------------------------------------------
opencv-python>=4.10.0.84,<5
ffmpeg-python>=0.2.0,<0.3
vidgear>=0.3.3,<1
